define(function (require) {
    "use strict";

    require('./config');

    require('./services/base-wizard');

    require('./widgets/container/index');
    require('./widgets/component-type/index');
});
