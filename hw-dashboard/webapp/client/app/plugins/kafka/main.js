define(function (require) {
    "use strict";

    // load module definition
    require('./ngModule');

    // load module config

    // load module constants

    // load module services
    require('./services/restService');

    // load module models
    require('./models/kafka-topic');

    // widgets

});
