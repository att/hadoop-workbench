define(function (require) {
    "use strict";

    require('../ngModule').constant('user-settings.pages.aws-provision-settings-properties.storageKeys', {
        'AWS_PROVISION': 'awsProvision'
    });
});
